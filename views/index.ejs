<!DOCTYPE html>
<html>
    <head>
        <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
        <title>Henhouse Automatised</title>
        <link href="/favicon.ico" rel="shortcut icon" type="image/x-icon">
        <meta name="viewport" content="width=device-width">
        <link rel="stylesheet" href="/stylesheets/styles.css">
        <meta name="theme-color" content="#7A828D">
        <meta name="msapplication-navbutton-color" content="#7A828D">
        <meta name="apple-mobile-web-app-capable" content="yes">
        <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    </head>
    <body ng-app="HenhouseAutomatised" ng-swipe-right="$menu.hide()" ng-swipe-left="$menu.show()">
        <div class="site-container">
            <div class="site-pusher">
                <header class="header">
                    <a class="header-icon" ng-click="$menu.show()"></a>
                    <nav class="menu">
                        <a ng-click="$menu.hide('/')"  ng-show="user" ng-class="{active: nav == 'controls'}"><span class="icon-sliders"></span> Controls</a>
                        <a ng-click="$menu.hide('/users')"  ng-show="user" ng-class="{active: nav == 'users'}"><span class="icon-user"></span> Users</a>
                        <a ng-click="$menu.logout()"  ng-show="user"><span class="icon-off"></span> Logout</a>
                        <a ng-click="$menu.hide('/login')" ng-show="nav === 'signup'"><span class="icon-user"></span> Login</a>
                    </nav>
                    <span class="header-logo">Henhouse Automatised</span>
                </header>

                <div class="site-content">
                    <div class="container" autoscroll="true" ng-view>
                        <noscript>
                            <h1>Houston, we have a problem.</h1>
                            <p>You must enable Javascript to use this applications.</p>
                        </noscript>
                    </div>
                </div>

                <div class="site-cache" ng-click="$menu.hide()"></div>
            </div>
        </div>

        <script type="text/javascript" src="/javascripts/scripts.js"></script>
    </body>
</html>
